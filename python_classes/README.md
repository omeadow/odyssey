Reading the definition of http library's classes (HTTPRequest), in particular the __init__ method made me me start tinkering with python class definitions, class instances (objects), how .self is treated, calling dir(), type() on each.

I will practice with those further but in this separate directory. 

The hope is to gain a very intuitive understanding of functions, classes, methods within classes; understand those as data types.

